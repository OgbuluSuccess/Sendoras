// ─────────────────────────────────────────────────────────────────────────────
// Central brand configuration.
// To rename the app: change the values below — the entire client updates.
// ─────────────────────────────────────────────────────────────────────────────

export const APP_NAME    = 'Sendhiiv';
export const APP_DOMAIN  = 'sendhiiv.com';
export const APP_URL     = `https://app.${APP_DOMAIN}`;
export const APP_EMAIL   = `sales@${APP_DOMAIN}`;
export const APP_SUPPORT = `support@${APP_DOMAIN}`;
