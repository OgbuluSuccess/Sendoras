// ─────────────────────────────────────────────────────────────────────────────
// Central brand configuration.
// To rename the app: change the values below — the entire server updates.
// ─────────────────────────────────────────────────────────────────────────────

const APP_NAME    = 'Sendhiiv';
const APP_DOMAIN  = 'sendhiiv.com';
const APP_URL     = `https://app.${APP_DOMAIN}`;
const APP_EMAIL   = `sales@${APP_DOMAIN}`;
const APP_SUPPORT = `support@${APP_DOMAIN}`;

module.exports = { APP_NAME, APP_DOMAIN, APP_URL, APP_EMAIL, APP_SUPPORT };
